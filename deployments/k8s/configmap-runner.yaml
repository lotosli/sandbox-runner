apiVersion: v1
kind: ConfigMap
metadata:
  name: sandbox-runner-config
  namespace: ai-sandbox-runs
data:
  run.yaml: |
    # replace with configs/run.sample.yaml content
  policy.yaml: |
    # replace with configs/policy.sample.yaml content
